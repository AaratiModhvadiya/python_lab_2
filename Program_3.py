def find_length_of_list(lst):
    length = len(lst)
    return length

# Example usage:
my_list = [1, 2, 3, 4, 5]
length_of_list = find_length_of_list(my_list)
print("Length of the list:", length_of_list)
